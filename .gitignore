# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!src/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Project specific
# Exclude snapshots folder (contains user-specific data extracts)
Vribbels/snapshots/

# Capture writes the player's own account data into api/snapshots/, so ignore the whole folder and
# list the reference JSON that is meant to be tracked. New capture output is then ignored by default.
api/snapshots/*
!api/snapshots/equip_effects.json
!api/snapshots/skill_eff_dictionary.json
!api/snapshots/skill_eff_schema.json
!api/snapshots/stat_validation_report.json

# Reference images and icon generation tools - not needed in repository
CZN Assets/
Vribbels/images/growth_stone_*.png
Vribbels/images/create_icon.py
Vribbels/images/generate_growth_stones.py
Vribbels/images/__pycache__/

# Claude Code files and internal documentation
.claude/
CLAUDE.md
RELEASE_CHECKLIST.md
PLAN.md

# Git worktrees (isolated workspaces for parallel development)
.worktrees/

# Build output (not needed in repository)
build/
dist/

# Release packages (distributed via GitHub Releases)
*.zip
Vribbels/Vribbels.zip

# Frontend
node_modules/
dist/
src-tauri/target/
src-tauri/binaries/*.exe

# Test artifacts
test_output.txt

# Raw extracted game data (bundled version is at api/data/game_db.json)
extracted_db/

# Superpowers internal files
.superpowers/
docs/superpowers/

# Local scripts
.scripts/
.builds/